[project]
name = "uv-fixture"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

# Uv is standard PEP-621 apart from it's additional dev-dependencies table
[tool.uv]
dev-dependencies = [
    "coverage[toml]"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
